<?xml version="1.0" encoding="UTF-8"?>
<CustomObject xmlns="http://soap.sforce.com/2006/04/metadata">
    <deploymentStatus>Deployed</deploymentStatus>
    <description>Custom form theme (rebuild, catalog §6): user-created theme-property JSON in the SAME shape as a built-in recipe. Built-ins hide in managed code; custom themes are records so they can be saved, listed, and shared (storage differs, the pipeline doesn't).</description>
    <label>Theme Definition</label>
    <nameField>
        <label>Theme Name</label>
        <type>Text</type>
    </nameField>
    <pluralLabel>Theme Definitions</pluralLabel>
    <sharingModel>ReadWrite</sharingModel>
</CustomObject>
