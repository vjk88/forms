<aura:application access="GLOBAL" extends="ltng:outApp">
    <!-- SLICE 0 SPIKE — throwaway. Mirrors finalStudioOut exactly so the
         native record picker is tested under the REAL Studio host
         (Lightning Out inside Visualforce), not inferred from LEX. -->
    <aura:dependency resource="c:finalLookupSpike" />
</aura:application>
